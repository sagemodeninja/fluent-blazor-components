@using System.Text.Json
@using sagemode_fluent_blazor_components.Utilities.Classes

@inherits FluentComponentBase

<fluent-navigation-view @ref="Element"
                        pane-display-mode="@PaneDisplayMode?.ToString().ToLower()"
                        header="@Header"
                        pane-title="@PaneTitle"
                        always-show-header="@AlwaysShowHeader.ToString().ToLower()"
                        is-settings-visible="@IsSettingsVisible.ToString().ToLower()"
                        selects-on-load="@SelectsOnLoad"
                        header-src="@HeaderSrc?.ToString().ToLower()"
                        @onselectionchanged="@OnSelectionChanged"
                        @attributes="AdditionalAttributes">
    @ChildContent
</fluent-navigation-view>