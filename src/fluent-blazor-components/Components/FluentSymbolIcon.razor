@inherits FluentFontIcon

<fluent-symbol-icon @ref="Element"
                  font-family="@FontFamily"
                  glyph="@Glyph"
                  font-size="@FontSize"
                  foreground="@Foreground"
                  symbol="@Symbol"
                  @attributes="@AdditionalAttributes">
    @ChildContent
</fluent-symbol-icon>